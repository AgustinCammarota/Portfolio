---
import { getLangFromUrl, useTranslations } from "@i18n/utils";
import ErrorLayout from "@layouts/ErrorLayout.astro";
import ErrorTitle from "@components/ErrorTitle.astro";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const title = t("internal.error.title");
const description = t("internal.error.description");
const keywords = t("internal.error.keywords");
const header = t("internal.error.header");
const link = t("internal.error.link");
---

<ErrorLayout title={title} description={description} keywords={keywords}>
  <ErrorTitle header={header} code="5☹5" link={link} />
</ErrorLayout>
